{
  "name": "chromosphere",
  "version": "0.0.0",
  "private": true,
  "description": "A headless component library for React, Vue, and other frameworks",
  "scripts": {
    "build": "pnpm -r --filter='./packages/*' run build",
    "dev:react": "pnpm --filter react-example dev",
    "dev:vue": "pnpm --filter vue-example dev"
  },
  "keywords": [
    "headless",
    "ui",
    "components",
    "react",
    "vue"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}

